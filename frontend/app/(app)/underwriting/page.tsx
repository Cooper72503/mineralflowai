import UnderwritingPage from "./UnderwritingPage";

export const metadata = {
  title: "Underwriting | MineralFlow AI",
  description: "Operator Due Diligence Brain — structured underwriting for working interest acquisitions.",
};

export default function Page() {
  return <UnderwritingPage />;
}
